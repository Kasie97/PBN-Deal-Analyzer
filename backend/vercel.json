{
  "version": 2,
  "builds": [
    {
      "src": "api/analyze-deal.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/analyze-deal.ts"
    }
  ]
}